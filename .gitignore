node_modules/
.federation-sandbox/
*.db
.env
dist/
build/
coverage/
.DS_Store
*.log
.vscode/
.idea/

# Nested repository clones (e.g. OpenClaw fork kept side-by-side for PR work).
# These should live outside the project or be properly gitignored to avoid
# polluting parent git history.
/openclaw/
